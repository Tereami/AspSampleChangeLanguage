# AspSampleChangeLanguage

A simple application demonstrating the use of localization and language switching.

It uses Resx files and standard Microsoft.AspNetCore.Localization.
The browser language is used for the first log in. Language switching is done using the buttons in the header. The selected language is saved in cookies.

Technologies stack: ASP.NET Core MVC, .NET 8.0.